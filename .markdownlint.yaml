default: true
MD013: false          # line length — disabled for prose
MD033: false          # inline HTML — needed for some platforms
MD040: false          # fenced-code-language — too noisy for blog content
MD041: false          # first line heading — frontmatter comes first
MD060: false          # table-column-style — too picky for blog tables
MD024:
  siblings_only: true # allow same heading text in different sections
